# EMLAKO Immobilier — maquettes Webminds

- `/` : version sombre (V1)
- `/light` : version claire (V2)

Site statique, aucun build. Deploy Vercel : framework = Other, build command vide, output directory = racine.
